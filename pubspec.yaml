name: flutter_devlog
description: A lightweight, zero-dependency, extensible colored console logger for Dart and Flutter with source location, JSON, levels, tags and scopes.
version: 0.1.0
homepage: https://github.com/bayajitislam/flutter_devlog
repository: https://github.com/bayajitislam/flutter_devlog
issue_tracker: https://github.com/bayajitislam/flutter_devlog/issues

topics:
  - logging
  - logger
  - debug
  - console

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
